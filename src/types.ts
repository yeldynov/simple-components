export interface AccordionItem {
  id: string;
  label: string;
  content: string;
}

export interface DropdownOption {
  label: string;
  value: string;
}

export interface MenuItem {
  label: string;
  path: string;
}
